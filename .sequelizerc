const path = require('path');

module.exports = {
  config: path.resolve('src/database', 'config.js'), // Arquivo não existe ainda - vamos criar
  'models-path': path.resolve('src/database/tabelas'), // Ajustado para sua pasta 'tabelas'
  'seeders-path': path.resolve('src/database/seeders'), // Pasta a ser criada
  'migrations-path': path.resolve('src/database/migrations') // Pasta a ser criada
};