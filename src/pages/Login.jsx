import React from 'react';
import LoginForm from '../components/LoginForm/loginForm';

const Login = () => {
    return (
        <div style={{ backgroundColor: '#008584', minHeight: '100vh', padding: '20px' }}>
            <LoginForm/>
        </div>
    );
};

export default Login;